spring:
  application:
    name: rabbitmq-binder
  cloud:
    stream:
      function:
        definition: myProducerSource;myConsumerSink
      bindings:
        pkslowSource-out-0:
         destination: my-topic
        pkslowSink-in-0:
          destination: my-topic
      poller:
        fixed-delay: 500